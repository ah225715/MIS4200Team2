@model IEnumerable<MIS4200Team2.Models.recognitions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserProfile.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfRecognition)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.values)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserProfile.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfRecognition)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.values)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.recognitionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.recognitionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.recognitionId })
        </td>
    </tr>
}

</table>
